DEFAULTS: &DEFAULTS
  settings: <%= ActiveSupport::JSON.encode(Board::Settings[3, 3, 1]) %>

win1_board:
  <<: *DEFAULTS
  game: win1

loss1_board:
  <<: *DEFAULTS
  game: loss1

standing_by1_board:
  <<: *DEFAULTS
  game: standing_by1

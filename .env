# Values in this file (.env):
# - Should be generic to all local + deployment environments.
# - Must NEVER contain sensitive data like API keys or passwords.
#   (This is what encrypted credentials are for.)

<% title("New Custom Game") %>

<div class="container mx-auto space-y-24">
  <h1><%= title %></h1>

  <%= turbo_frame_tag(@view.turbo_frame_name) do %>
    <%= render("form", form: @view.form(context: layout)) %>
  <% end %>
</div>

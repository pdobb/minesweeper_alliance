<%# locals: (container:) %>

<div class="container mx-auto md:mt-24 space-y-16">
  <% cache(
       container.title_cache_key,
       expires_in: container.title_cahce_expires_in,
     ) do %>
    <h1 class="text-center"><%= container.title %></h1>
  <% end %>

  <%= render("games/new/content", content: container.content) %>
</div>

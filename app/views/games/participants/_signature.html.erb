<%# locals: (signature:) %>

<%= turbo_frame_tag(*signature.turbo_frame_name) do %>
  <div class="flex items-center gap-x-3">
    <%= signature.attribution_prefix %>

    <%= link_to(
          signature.attribution_link_content,
          signature.attribution_link_url,
          data: { turbo_stream: true }) %>
  </div>
<% end %>

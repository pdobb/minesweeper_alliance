<%# locals: (user:) %>

<%= turbo_frame_tag(user) do %>
  <%= link_to(
        user.display_name,
        edit_user_path,
        class: "edit-link",
        title: "Edit username",
        data: { turbo_stream: true }) %>
<% end %>

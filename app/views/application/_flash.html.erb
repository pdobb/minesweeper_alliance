<%# locals: (flash:) %>

<div
  id="<%= flash.turbo_target %>"
  class="
    pointer-events-none z-10
    fixed top-16 inset-x-0
    px-12
    flex flex-col items-center gap-y-3
  "
>
  <%= render(
        partial: "application/flash/notification",
        collection: flash.notifications,
      ) %>
</div>

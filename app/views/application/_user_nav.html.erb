<%# locals: (user_nav:) %>

<%= turbo_frame_tag(*user_nav.turbo_frame_name) do %>
  <% if user_nav.participant? %>
    <%= active_link_to(
          user_nav.profile_url,
          data: { turbo_frame: :_top }) do %>
      <span class="max-md:hidden <%= user_nav.turbo_update_id %>">
        <%= user_nav.display_name %>
      </span>
      <%= Emoji.medal %>
    <% end %>
  <% else %>
    <span id="unsignedUserNav" class="max-md:hidden">
      <%= t("site.name") %>
    </span>
  <% end %>
<% end %>

<%# locals: () %>

<div
  class="
    container mx-auto
    flex max-lg:flex-col gap-y-6
    justify-between items-center
  "
>
  <nav class="lg:order-2 max-lg:w-full">
    <ul class="flex justify-between items-center gap-x-24 lg:gap-x-8">
      <li><%= active_link_to("About", about_path) %></li>
      <li class="max-lg:hidden border-l border-dim self-stretch"></li>
      <li><%= render("layouts/theme_selector") %></li>
    </ul>
  </nav>

  <small class="text-center text-dim lg:order-1">
    &copy; <%= Date.today.year %> Minesweeper Alliance
    <span class="font-sans text-dim-lg mx-1">&mdash;</span>
    Made with
    <span class="opacity-80"><%= Emoji.heart %></span>
    by the <%= t("footer.maker_html").sample %>
  </small>
</div>

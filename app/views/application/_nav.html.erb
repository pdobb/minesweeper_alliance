<%# locals: () %>

<nav class="container mx-auto flex justify-between">
  <ul class="flex gap-x-6 lg:gap-x-12">
    <li><%= active_link_to("War Room", root_path) %></li>
    <li>
      <%= active_link_to(
            "Sweep Ops Archives", games_path, includes: GamesController.name) %>
    </li>
  </ul>

  <%= render("application/username") %>
</nav>
